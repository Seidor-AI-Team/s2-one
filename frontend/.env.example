# ── S2-ONE Frontend Environment Variables ──

# API token — must match backend API_TOKEN
VITE_API_TOKEN=s2one-hackathon-2026

# Admin token — must match backend ADMIN_TOKEN (for config panel)
VITE_ADMIN_TOKEN=s2one-admin-2026
